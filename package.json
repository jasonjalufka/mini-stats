{
  "name": "mini-stats",
  "version": "1.0.0",
  "description": "A node.js microservice to grab stats from the web",
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "dev": "micro-dev",
    "deploy": "now && now alias",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jason Jalufka <jasonjalufka@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.1.0",
    "micro": "^9.3.4",
    "node-fetch": "^2.6.0",
    "now-micro": "^0.4.0"
  },
  "devDependencies": {
    "micro-dev": "^3.0.0"
  }
}
